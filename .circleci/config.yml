version: 2.1

jobs:
  build-for-linux:
    docker: 
      - image: cimg/node:14.10.1
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run: make clean
      - run: make all
      - run: cd bin
      - run: ./example1
      
 

workflows:
  version: 2.1
  build:
    jobs:
      - build-for-linux
